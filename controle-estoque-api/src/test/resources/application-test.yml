spring:

  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop

  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin123

  data:
    redis:
      host: localhost
      port: 6379


api:
  security:
    token:
      secret: teste-secret